<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sandrine Coupart - Diététicienne Nutritionniste{% endblock %}</title>
        <link rel="icon" href="images/flaticon.png" rel="stylesheets">
        <meta name="viewport" content="with=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
       
        <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet">   

        <link href="{{ asset('assets/css/style.css') }}" rel="stylesheet">
        
        {% block stylesheets %}{% endblock %}
        {% block javascript %}
        {% endblock %}
            
    </head>

    <header>

        <nav class="navbar navbar-expand-lg navbar-light">
          <a class="navbar-brand" href="{{ path('app_home_page') }}"><img src= "{{ asset('images/SandrineCoupart.png') }}" class="img-fluid logo" alt="logo" width="160px" height="120px"/></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        
          <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
              <li class="nav-item">
                <a class="nav-link text-light"  href="{{ path('app_home_page') }}">Accueil</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light"  href="{{ path('app_home_page', {'_fragment' :'a_propos'}) }}">A propos</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('app_home_page', {'_fragment' :'mes_services'}) }}">Mes services</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('app_recipes') }}">Idées recettes</a>
              </li>
              <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('app_contact') }}">Contact</a>
              </li>
            </ul>
            <div class="navbar-item-custom">
                {% if app.user %}
                  <a href="{{ path('app_account') }}">Mon compte <small>({{ app.user.firstname }})</small></a> | <a href ="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                  <button class="btn btn-success my-2 my-sm-0" type="submit">
                    <a class="text-light" href="{{ path('app_login') }}">S'identifier</a>
                  </button>
                {% endif %}
          </div>
        </nav>
  
    </header>

    <body>

      <footer class="fixed-bottom mt-5">
          <p>SANDRINE COUPART - Diététicienne Nutritionniste diplomés d'Etat - Editer par Laetitia DECRE © Tous droits réservés - 2022 -  
          <small><a class="text-light" href="{{ path('app_rgpd') }}"> Mentions Légales et Politique de confidentialité</a></small>
          </p>
      </footer>

      <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
      <script src="{{ asset('assets/css/bootstrap.bundle.min.js') }}"></script> 
      <script src="{{ asset('assets/build/app.js') }}"></script>

        {% block body %} {% endblock %}
       
    </body>

        
    
</html>



